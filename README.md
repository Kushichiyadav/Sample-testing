/* ============================= */
/* QUESTION 4 COMPLETE SOLUTION  */
/* ============================= */


/* 1. CREATE CUSTOMER TABLE */

CREATE TABLE CUSTOMER (
    CUST_ID INT PRIMARY KEY,
    CUST_NAME VARCHAR(50) NOT NULL,
    CUST_ADD VARCHAR(100)
);


/* 2. CREATE SALES TABLE WITH CONSTRAINT PAYMENT_MODE = COD OR PAYPAL */

CREATE TABLE SALES (
    CUSTOMER_ID INT,
    ITEM_ID INT,
    PAYMENT_MODE VARCHAR(10),

    PRIMARY KEY (CUSTOMER_ID, ITEM_ID),

    FOREIGN KEY (CUSTOMER_ID)
    REFERENCES CUSTOMER(CUST_ID),

    CHECK (PAYMENT_MODE IN ('COD','PAYPAL'))
);


/* 3. INSERT 5 RECORDS INTO CUSTOMER */

INSERT INTO CUSTOMER VALUES (101,'Rahul','Delhi');
INSERT INTO CUSTOMER VALUES (102,'Aman','Mumbai');
INSERT INTO CUSTOMER VALUES (103,'Priya','Pune');
INSERT INTO CUSTOMER VALUES (104,'Neha','Chennai');
INSERT INTO CUSTOMER VALUES (105,'Ravi','Jaipur');


/* 4. INSERT 5 RECORDS INTO SALES */

INSERT INTO SALES VALUES (101,1,'COD');
INSERT INTO SALES VALUES (102,2,'PAYPAL');
INSERT INTO SALES VALUES (103,3,'COD');
INSERT INTO SALES VALUES (104,4,'PAYPAL');
INSERT INTO SALES VALUES (105,5,'COD');


/* 5. ALTER DATATYPE OF CUST_ADD FROM VARCHAR TO CHAR */

ALTER TABLE CUSTOMER
MODIFY CUST_ADD CHAR(100);


/* 6. CREATE DDL TRIGGER FOR CREATE / ALTER / DROP EVENTS */

CREATE OR REPLACE TRIGGER DDL_TRIGGER_EXAMPLE
AFTER CREATE OR ALTER OR DROP
ON SCHEMA
BEGIN
    DBMS_OUTPUT.PUT_LINE('DDL EVENT OCCURRED IN SCHEMA');
END;
/
